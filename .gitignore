/vlessvmore
/dist/
/private

# A real deployment's own files, if you run one from this checkout.
# data/ holds the Reality private key and user UUIDs; backups/ holds copies of both.
/docker-compose.yml
/config
/data
/backups
